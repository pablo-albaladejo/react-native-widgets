import { Dimensions } from 'react-native';

export const { height, width } = Dimensions.get('window')
export const marginHotizontal = Dimensions.get('window').width * 0.05;
export const marginVertical = Dimensions.get('window').height * 0.05;